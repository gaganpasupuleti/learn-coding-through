import { PowerBiLandingPage } from './PowerBiLandingPage'

/**
 * Entry point for /practice/powerbi — Phase 24A landing hub only.
 * Sub-routes (DAX IDE, Quiz) will be added in later phases.
 */
export function PowerBiPracticePage() {
  return <PowerBiLandingPage />
}
